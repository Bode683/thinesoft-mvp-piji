global:
  # smtp_smarthost: 'localhost:587'
  # smtp_from: 'alertmanager@theddt.local'
  # smtp_auth_username: ''
  # smtp_auth_password: ''

route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'default-receiver'

receivers:
  - name: 'default-receiver'
    # Configure email notifications
    # email_configs:
    #   - to: 'admin@theddt.local'
    #     subject: 'Alert: {{ .GroupLabels.alertname }}'
    #     body: |
    #       {{ range .Alerts }}
    #       Alert: {{ .Annotations.summary }}
    #       Description: {{ .Annotations.description }}
    #       {{ end }}
    
    # Configure webhook notifications (disabled by default for dev)
    # webhook_configs:
    #   - url: 'http://localhost:5001/webhook'
    #     send_resolved: true

inhibit_rules:
  - source_match:
      severity: 'critical'
    target_match:
      severity: 'warning'
    equal: ['alertname', 'dev', 'instance']
